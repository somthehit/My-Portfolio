# Database
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"

# Supabase (for resume upload)
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_STORAGE_BUCKET="public"

# Public
NEXT_PUBLIC_CONTACT_EMAIL="hello@example.com"

# Admin auth (no public signup)
ADMIN_EMAIL="admin@example.com"
# Generate this with: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
ADMIN_PASSWORD_HASH=""

# JWT signing secret for admin session cookie
JWT_SECRET="change-me-to-a-long-random-string"
